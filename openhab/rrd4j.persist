// persistence strategies have a name and a definition and are referred to in the "Items" section
Strategies {
	// for rrd charts, we need a cron strategy
	everyMinute : "0 * * * * ?"

	// every day at 00:00:50
	everyDay : "50 0 0 * * ?"
}

Items {
    // Bedroom_Lights_Switch_Daily_Usage is persisted by rules, so not placed here.
}
